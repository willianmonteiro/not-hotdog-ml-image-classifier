# Python dependencies for the model training/evaluation pipeline.
# Install with:  pip install -r model/requirements.txt

# TensorFlow bundles Keras — our CNN framework and the ImageDataGenerator preprocessing.
tensorflow>=2.13,<2.17

# Pillow: the image library TensorFlow uses under the hood to decode JP/PNG files.
pillow>=10.0

# NumPy: array math; image batches are NumPy arrays.
numpy>=1.24

# Matplotlib + scikit-learn are used in step 6 (evaluation): loss curves and confusion matrix.
matplotlib>=3.7
scikit-learn>=1.3
