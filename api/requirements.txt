# API dependencies. Install with: pip install -r api/requirements.txt

fastapi>=0.110          # web framework + automatic /docs
uvicorn[standard]>=0.29 # ASGI server that runs the app
python-multipart>=0.0.9 # required by FastAPI to parse uploaded files
pillow>=10.0            # decode the uploaded image
numpy>=1.24            # array the image before inference
tensorflow>=2.13,<2.17 # load and run the trained model
